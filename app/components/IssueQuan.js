import React, { Component, PropTypes } from 'react';

export default class QuanBatchList extends Component {
  constructor(props) {
    super(props);
  }

  render () {
    return (
      Hi, there.
    );
  }
}
